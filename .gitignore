.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/extensions.json
.vscode/launch.json
.vscode/ipch
src/secrets_override.h
**/.DS_Store

# Nix / direnv
.direnv
result

# PlatformIO uses the global ~/.platformio. This only catches the case where
# somebody points PLATFORMIO_CORE_DIR at the checkout again.
.platformio
compile_commands.json

# written by the snapshot test when the command table changed
test/test_command_snapshot/commands.actual.txt

# simulator runtime data: the local stand-in for the littlefs partition
omote_data/

# where omotectl pull writes by default
cfg_backup/
